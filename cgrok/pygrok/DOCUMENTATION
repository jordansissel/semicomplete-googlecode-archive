pygrok usage:

import pygrok

patterns = {
  "WORD": r"\w+",
  ...
}

g = pygrok.GrokRegex()
g.add_patterns(patterns)
g.set_regex("Hello %WORD%")
m = g.search("Hello world")

# m["WORD"] == "world"
# m["=LINE"] == "Hello world"
# ...
