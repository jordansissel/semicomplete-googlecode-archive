hosts:
  snack.home:
    address: 192.168.0.97
    services:
    - pingable
    - env
  dell:
    address: 192.168.0.22
    services:
    - pingable
    - env
  google:
    address: www.google.com
    services:
    - pingable

services:
  pingable:
    #tests:
    #- ping
  webserver:
    tests:
    - http
  env:
    tests: 
    - env
  
tests:
  ping: fping -c 1 "$HOST_ADDRESS"
  http: /usr/lib/nagios/plugins/check_http -H "$HOST_NAME"
  env: env | egrep "^(HOST|SERVICE|TEST)_" | sort
